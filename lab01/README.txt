code1和code2都是图像缩放的代码，都可以使用

图像缩减：code1与code2实现的是一样的基于朴素接缝裁剪的算法，缩减代码一致

图像拉伸：code2为优化的接缝裁剪算法，code1在图像缩减的基础上使用了一些小trick，code1比code2图像拉伸效果更好

程序使用方法：比如想要横向缩减100像素，需要将code1与code2第二行change_size改为-100即可，横向放大为正。